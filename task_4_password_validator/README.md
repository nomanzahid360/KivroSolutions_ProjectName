# Task 4 – Simple Password Validator

This Python program validates user passwords based on three conditions:
1. Password length must be at least **6 characters**.
2. Password must contain **at least one number**.
3. Password must contain **at least one capital letter**.

## 🚀 How to Run

1. Open your terminal or IDE (e.g., Cursor, VS Code, PyCharm).
2. Run the program:

   ```bash
   python password_validator.py